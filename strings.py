
   # pseudo code
    #Declare a varibale by assigning a string with strip
    #Than apply string strip() function to remove the specific sign from string
    #Display the output without strip
# Declare a variable by assigning the string
hero="$$$Superman$$$"
#printing the output without $ sign by using strip function to remove the strip
print(hero.strip("$"))

    
    